#!/usr/bin/env ruby
# callowaylc@gmail
# defines tasks related to saltstack pillar operations

namespace :pillar do
  desc "Encrypts ./pillar/* to ./pillar"
  task :encrypt do

  end

  desc "Decrypts ./.pillar to ./pillar"
  task :decrypt do

  end
end
